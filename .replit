run = "python -m pip install --upgrade pip setuptools wheel && pip install -r requirements.txt && gunicorn app:app -w 2 -b 0.0.0.0:$PORT --log-level info"

[env]
FLASK_DEBUG = "false"

[nix]
channel = "stable-23_05"

[[ports]]
localPort = 5000
externalPort = 80
